{
  "name": "google-oauth2-token-info",
  "version": "1.1.0",
  "description": "Returns oauth2 v3 token info",
  "main": "./build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets babel-preset-es2015 --out-dir build",
    "prepublish": "npm run build"
  },
  "repository" : {
    "type" : "git",
    "url" : "https://github.com/alopes/google-oauth2-token-info"
  },
  "author": "",
  "license": "MIT License",
  "dependencies": {
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1"
  }
}
